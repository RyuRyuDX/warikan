# loop-harness 設定（verify.sh が source する）
WATCH_GLOBS=('*.ts' '*.tsx' '*.js' '*.jsx')
CHECKS=('npx tsc --noEmit' 'npm run lint')
TEST_CMD=''
INCLUDE_TEST=0    # 1 で CHECKS 末尾にテストも追加（厳格・低速）
REQUIRE_CHANGES=0 # 1 で「差分ゼロでの完了」もブロック（空diff拒否）
